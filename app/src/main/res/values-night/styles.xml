<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/name</item>
        <item name="colorPrimaryDark">@color/name</item>
        <item name="colorAccent">@color/attack</item>
    </style>

</resources>